#ifndef _BASIC_H_
#define _BASIC_H_

#include <algorithm> // 添加头文件以使用 std::rotate
#include <cmath>     // Include this at the top of your file
#include <fstream>
#include <iomanip> // 对于 std::left, std::right 和 std::setw
#include <iostream>
#include <list>
#include <map>
#include <memory>
#include <string>

#include <vector>

#include <cassert>
#include <stdio.h>
#include <typeinfo>

#include <ctime>

typedef unsigned long long AddrType;

#endif